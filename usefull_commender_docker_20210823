
20210823

docker 설치(docker 홈페이지 참조)
docker compose 설치(docker 홈페이지 참조)
git 설치(git 홈페이지 참조)
git clone yolo(git 홈페이지 참조)
deepch-engine-yolo로 이동
sudo docker-compose build
error
sudo docker login jdlab.azurecr.io
Username: jdlab
Password: 대표님께 문의
sudo docker-compose build
sudo docker-compose push
docker-compose(실서버에 반영 되는 compose 파일 사용)
sudo docker-compose -f docker-compose-dev.yml build(테스트서버에 반영 되는 compose 파일 사용)

테스트서버에 weight를 업데이트 해야 하는 경우, 중무로에 있는 테스트서버 컴퓨터에 가서
build, push, compose를 해야한다.

여기 까지 실서버 weight 파일 업데이트 하는 방법이었다.
