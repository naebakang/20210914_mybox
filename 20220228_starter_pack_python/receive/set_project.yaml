# Set project

# 프로젝트가 pyinstaller 를 통해 설치 파일로 만들어지면, env.py 를 수정 할 수가 없다.
# env.py 값 중에 프로젝트가 설치 파일임에도 수정 할 수 있게 하고 싶은 값을
# env.py 대신 여기에 기록한다.
# 또한, 이미 실행되고 있는 코드에 실시간으로 변경된 값을 주고자 할 때 여기에 기록한다.

check_add_b_hoop: False

